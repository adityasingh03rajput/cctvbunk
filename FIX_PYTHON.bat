@echo off
powershell -NoProfile -ExecutionPolicy Bypass -Command "$pythonPath = 'C:\Users\Victus\AppData\Local\Programs\Python\Python314'; $scriptsPath = \"$pythonPath\Scripts\"; $userPath = [Environment]::GetEnvironmentVariable('Path', 'User'); $changed = $false; if ($userPath -notlike \"*$pythonPath*\") { $userPath = \"$pythonPath;$userPath\"; $changed = $true; }; if ($userPath -notlike \"*$scriptsPath*\") { $userPath = \"$scriptsPath;$userPath\"; $changed = $true; }; if ($changed) { [Environment]::SetEnvironmentVariable('Path', $userPath, 'User'); Write-Host '✅ Added Python to User PATH permanently.' -ForegroundColor Green; } else { Write-Host 'ℹ️ Python is already in your PATH.' -ForegroundColor Cyan; }; $alias1 = \"$env:LOCALAPPDATA\Microsoft\WindowsApps\python.exe\"; $alias2 = \"$env:LOCALAPPDATA\Microsoft\WindowsApps\python3.exe\"; if (Test-Path $alias1) { Remove-Item $alias1 -Force -ErrorAction SilentlyContinue }; if (Test-Path $alias2) { Remove-Item $alias2 -Force -ErrorAction SilentlyContinue }; Write-Host '✅ Disabled Microsoft Store Python Aliases.' -ForegroundColor Green; Write-Host ''; Write-Host '⚠️  IMPORTANT: You must RESTART VSCODE completely for the changes to take effect!' -ForegroundColor Yellow;"
pause
